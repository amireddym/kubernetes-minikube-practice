apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: dashboard-ingress
  namespace: 
spec:
  rules:
  - host: 
    http:
      paths: 
        backend:
          serviceName:
          servicePort:
  - host:
    http:
      paths:
        backend:
          serviceName:
          servicePort:
